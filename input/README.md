# ディレクトリ構造の説明

このファイルは `input/` ディレクトリが空ではないことを示すためのものです。

## 使い方

1. このディレクトリに結合したいPDFファイルを配置してください
2. `run.sh merge` を実行すると、`merged_output/` に結合されたPDFが生成されます

## 例

```
input/
├── A.pdf
├── B.pdf
└── C.pdf
```

これらのファイルが `merged_output/Merged.pdf` として結合されます。
